# pnpm 11 reads repo settings from here and refuses to run any dependency's
# install script until it is listed. Only these two need one: esbuild fetches
# its native binary, msw writes the service-worker file into public/.
allowBuilds:
  esbuild: true
  msw: true
